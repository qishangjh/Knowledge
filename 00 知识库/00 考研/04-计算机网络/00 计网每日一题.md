---
科目:
课程名称:
tags: []
一轮复习情况: 进行中
二轮复习情况: 未开始
三轮复习情况: 未开始
难度:
考频:
备注:
---

# 00 计网每日一题
from 9.06

## 1. 以下使用点分十进制形式 0.0.0.0 的是（）
    默认路由中的目的网络地址  
    封装 DHCP DISCOVER 报文的 IP 分组的目的地址   #DHCP  
    OS PF 主干区域的标识符

* **A**. 0.0.0.0 来表示"所有未知的网络"，也就是"任何网络"。**默认路由中的目的网络地址，就是 0.0.0.0**。
* **B**.   **DHCP Discover（发现）**：DHCP客户机广播"DHCP发现"消息，试图找到网络中的DHCP服务器，以便从DHCP服务器获得一个IP地址。**（源IP：0.0.0.0；目的IP：255.255.255.255；源MAC：客户端MAC；目的MAC：`FF-FF-FF-FF-FF-FF`）**
* **C. OSPF 主干区域的标识符** OSPF（开放最短路径优先）是个高级的路由协议，它把一个大的自治系统（AS）划分成很多小区域（Area），这样管理起来更高效。
    * 在OSPF里，有一个特殊的区域叫做**主干区域（Backbone Area）**，也叫区域0。这个区域是其他所有非主干区域的"交通枢纽"，所有的非主干区域都必须通过主干区域才能互相通信。
    * 主干区域的**标识符**是一个特殊的数字，用来唯一标识这个区域。而这个标识符，就是 **0**。在点分十进制形式下，OSPF主干区域的标识符就是 **0.0.0.0**。它代表了OSPF网络中的"核心地带"！
![[考研学习笔记04-计算机网络 00 计网每日一题 2025-10-11 0855.png]]
---

## 2. 以下属于 DHCP 客户发送的报文是
    1. DISCOVER
    2. OFFER
    3. REQUEST
    4. RELEASE  
![[考研学习笔记04-计算机网络 00 计网每日一题 2025-10-11 0855.png]]
* **动态主机配置协议DHCP**： #DHCP
    *   **提供即插即用的联网方式**，允许一台计算机加入新的网络时能被动态地分配IP地址而不用手动参与。DHCP是**应用层协议**，基于UDP传输，服务器监听**67号端口**，客户端监听**68号端口**。
    *   **工作原理（四报文交互过程）**：
        1.  **DHCP Discover（发现）**：DHCP客户机广播"DHCP发现"消息，试图找到网络中的DHCP服务器，以便从DHCP服务器获得一个IP地址。**（源IP：0.0.0.0；目的IP：255.255.255.255；源MAC：客户端MAC；目的MAC：`FF-FF-FF-FF-FF-FF`）**
        2.  **DHCP Offer（提供）**：DHCP服务器收到"DHCP发现"消息后，向网络中广播"DHCP提供"消息，其中包含提供给DHCP客户机的IP地址和相关配置信息。
        3.  **DHCP Request（请求）**：DHCP客户机收到"DHCP提供"消息，如果接收DHCP服务器所提供的相关参数，通过广播"DHCP请求"消息向DHCP服务器请求提供IP地址。
        4.  **DHCP ACK（确认）**：DHCP服务器广播"DHCP确认"消息，将IP地址分配给DHCP客户机。

---
## 3. 在 PPP 链路建立过程中，鉴别成功后进入的状态是（）
    1. 链路静止 (Link Dead)
    2. 链路建立 (Link EstabIishment)
    3. 网络层协议配置（Network-Layer Protocol)
    4. 打开 (Opened)

![[考研学习笔记04-计算机网络 00 计网每日一题 2025-10-12 1903.png]]

---
## 4. DHCP 客户为了获取 IP 地址，在与 DHCP 服务器交互时，正确的报文交换顺序是（）
       A. REQUEST—>OFFER —>DISCOVER —>ACK
       B. REQUEST—>ACK ->DISCOVER —>OFFER
       C. DISCOVER->OFFER —>REQUEST —>ACK
       D. DISCOVER->ACK ->REQUEST ->OFFER
![[考研学习笔记04-计算机网络 00 计网每日一题 2025-10-12 2232.png]]

---
## 5.  下列协议使用传输层无连接不可靠服务的是
    l. DHCP
    2. POP3 
    3. HTTP/1.x/2
    4. FTP
![[考研学习笔记04-计算机网络 00 计网每日一题 2025-10-12 2726.png]]

---
